# todo: create a profile builder

# Retrieval pattern (parent–child)

# Embed the job summary; search the vector store for top-K bullets.

# For each retrieved bullet, join its parent (company/title/dates) from SQLite/JSON before prompting.

# Optionally group by parent to enforce variety (e.g., max 3 bullets/role).